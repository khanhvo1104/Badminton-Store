/// Contract for Supabase PostgREST / database access.
///
/// Implementations will wrap `supabase.from(...)` in a later milestone.
/// No query methods are implemented in Shop Foundation.
abstract interface class SupabaseDatabase {
  // Typed table accessors and RPC helpers will be declared
  // when catalog, cart, and order tables are connected.
}
